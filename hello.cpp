/*
 * Course: C++ Programming: Basic Skills
 * Completion Time: 1.6 hrs, 29 October 2025.
 * Code written by Charles Underwood
 * Compiled using GCC: g++ hello.cpp -o hello
 * This file contains code for the course and my own additions.
 */

// Libraries
#include <iostream>
#include <math.h>
using namespace std;

// Main Function
int main () {

    // Print
    std::cout << "Hello World!" << endl;

    /*
    int a = 10;
    double b = 5.0;

    printf("%f \n", a/b);
    a++;
    printf("%d \n", a);

    string s1 = "I understand";
    string s2 = s1 + " string concatenation!";
    cout << s2 << endl;

    int num1 = 1;
    int num2 = 5;

    cout << (num1*num2) << endl;
    cout << (num1*=num2) << endl;

    cout << 9 / 3 + (100 % 100) - 3 << endl;
    cout << (5 % 50/20) << endl;
    cout << (5 % 50) << endl;
    // 3 + 0 - 3;
    */

    /*
    double a = 0.01;
    //bool a = true;
    cout << boolalpha << (bool) (int) a << endl;
    */

    // At this stage, I'm writing my own code.
    /*
    if (1 == 1) {
        cout << "1 = 1 is true" << endl;
    } else if (2 >= 1) {
        cout << "2 >= 1 is true" << endl;
    } else {
        cout << "unknown" << endl;
    }
    */

    /*
    int emotion = 0;
    switch (emotion) {
        case 1:
            cout << "happy" << endl;
            break;
        case 2:
            cout << "sad" << endl;
            break;
        case 3:
            cout << "angry" << endl;
            break;
        default:
            cout << "unknown" << endl;
    }
    */

    /*
    int com = 0;
    while (com != 1) {
        for (int x = 0; x < 10; x++) {
            for (int y = 0; y < x; y++) {
                cout << "current coordinate: " << "(" << x << ", " << y << ")" << endl;
            }
        }
        com = 1;
    }
    */

    /*
    string fruits[6] = {"pineapple", "apple", "pear", "strawberry", "mango", "blueberry"};
    cout << fruits[0] << " on pizza is good." << endl;
    */

    // Successful run
    return 0;

}
